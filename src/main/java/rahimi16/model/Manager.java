package rahimi16.model;

public class Manager extends Employee{
    public Manager() {
    }

    public Manager(String nId, String name, Double salary, Double taxRate) {
        super(nId, name, salary, taxRate);
    }
}
